class Solution {
    public int largestAltitude(int[] gain) {
        int n=gain.length;
int max=0;
int val=0;
        for(int i=0;i<n;i++){
            max+=gain[i];

            val=Math.max(val,max);
        }
        return val;
    }
}
