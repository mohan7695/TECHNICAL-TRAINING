class Solution {
    public int reverseDegree(String s) {
       
        int sum = 0;

        for (int i = 0; i < s.length(); i++) {
           int g = s.charAt(i)-'a';
            int in = 26-g;
            sum += in * (i + 1);
        }
        return sum;
    }
}
