class Solution {
    public int minimumPushes(String word) {
        int sum = 0;
        for (int i = 0; i < word.length(); i++) {
           
          sum+= i/8+1;

        }
        return sum;
    }
}


class Solution {
    public int minimumPushes(String word) {
        int sum = 0;
        for (int i = 1; i <= word.length(); i++) {
           
          if (i <= 8) {
                sum += 1;
            } else if ( i <= 16) {
                sum += 2;
            } else if ( i <= 24) {
                sum += 3;
            } else if ( i <=26) {
                sum += 4;
            }

        }
        return sum;
    }
}
