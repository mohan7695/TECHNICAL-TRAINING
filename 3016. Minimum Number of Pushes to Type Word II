 int[] arr = new int[26];
        for (char ch : word.toCharArray()) {
            arr[ch - 'a']++;
        }

        int ans = 0;
        int count = 0;

        Arrays.sort(arr);
        for (int i = 25; i >= 0; i--) {
            if (arr[i] == 0){

                break;
            }

            ans += arr[i] * (count / 8+1);
            count++;
        }

        return ans;
    }
}
